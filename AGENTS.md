# Pinter 项目协作指南

本文件是仓库级编码代理运行手册，适用于本目录及所有子目录。开始修改前先阅读本文件；ArkTS 规则以当前 DevEco Studio 和 SDK 文档为准，不在本文件中重复整套通用规范。

## 1. 项目概况

- 产品：Pinter 灵感画板原型，当前不内置灵感内容或真实账号数据。
- 平台：HarmonyOS，Stage 模型，单 `entry` HAP 模块。
- SDK：`targetSdkVersion` 与 `compatibleSdkVersion` 均为 `6.1.1(24)`。
- 设备：`phone`、`tablet`。
- 应用标识：`com.Pinter.app`；当前版本 `1.0.0` / `1000000`。
- 入口链路：`EntryAbility.onWindowStageCreate()` 加载 `pages/Index`。
- 当前无业务网络依赖，也没有真实账号、云同步、消息或图片上传服务。

## 2. 目录与职责

```text
AppScope/app.json5                         应用标识、版本、图标和名称
build-profile.json5                        产品、SDK、构建模式和签名配置
entry/src/main/module.json5                entry 模块、设备类型和 Ability 声明
entry/src/main/ets/entryability/           UIAbility 入口
entry/src/main/ets/entrybackupability/     系统备份扩展占位实现
entry/src/main/ets/models/                 业务数据接口
entry/src/main/ets/pages/                  页面和顶层交互
entry/src/main/ets/services/               本地存储与系统服务
entry/src/main/resources/                  字符串、颜色、页面清单和媒体资源
entry/src/test/                            本地单元测试
entry/src/ohosTest/                        设备侧测试
```

关键文件的所有权边界：

- `pages/Index.ets`：顶层页面与交互协调器。拥有首页、发现、发布、通知、我的五个标签页，以及搜索、灵感详情、画板选择、新建画板和反馈消息状态。
- `services/BoardStorageService.ets`：通过 Preferences 读取和保存画板、收藏 ID；页面不得复制另一套持久化实现。
- `models/InspirationModels.ets`：`Pin`、`Board`、`Profile`、`LocalBoardState` 的权威数据契约。跨层字段变更从这里开始。
- `entryability/EntryAbility.ets`：Ability 生命周期、颜色模式和 `pages/Index` 加载，不承载页面业务状态。
- `entrybackupability/EntryBackupAbility.ets`：当前只记录系统备份/恢复回调，不等同于完整的数据备份能力。

## 3. 当前能力边界

### 已实现

- 提供首页、发现、通知和我的页面骨架及空内容状态。
- 提供本地画板和收藏状态的 Preferences 读写服务。

### 原型占位

- 发布页不选择或上传真实图片，标题和描述也不会提交。
- 通知中的活动、消息和设置入口没有真实业务后端。
- 没有登录、关注、评论、联网内容加载、云同步或跨设备业务同步。
- 系统 `EntryBackupAbility` 只有空回调，不能宣称已经备份或恢复业务数据。

修改产品文案、帮助内容或测试名称时，必须遵守上述边界。未接入的能力应明确标为占位或暂不可用，不得写成已完成。

## 4. 数据与持久化

- Preferences 文件名：`inspira_boards`。
- 画板键：`boards`，JSON 内容为 `Board[]`。
- 收藏键：`saved_pin_ids`，JSON 内容为 `string[]`。
- 当前读取失败会回退为空状态；保存失败由 `BoardStorageService.save()` 返回 `false`，页面展示失败反馈。
- UI 状态数组采用重新赋值更新，例如 `this.boards = [...]` 或 `map()`，避免原地修改导致 ArkUI 状态未刷新。
- 新建画板 ID 当前使用 `board-${Date.now()}`；不要在没有迁移方案时改变已有 ID 或存储键含义。
- 修改 `Board`、`LocalBoardState` 或序列化格式时，必须同时检查旧数据解析、默认值、重复收藏处理和相关测试。
- 不在 Preferences、日志或本地服务中加入密码、令牌、API Key 等敏感信息。未来若增加凭证，应使用系统安全凭据能力并单独设计其生命周期。

## 5. ArkTS 与代码组织

- 保持严格类型；公共数据结构使用明确接口，避免 `any`、无依据的类型断言和结构不一致的匿名对象。
- 页面负责展示与交互协调，数据来源和持久化留在 `services/`，共享数据契约留在 `models/`。
- 优先使用当前 SDK 对应的 `@kit` 模块；已有 `@ohos` 导入只有在缺少等价 Kit API 或兼容性要求明确时保留。
- 异步存储调用必须处理成功与失败，不要先显示成功再忽略真实结果。
- `catch` 中不得静默吞掉会影响用户数据的错误；如需降级，返回可判断的结果并由 UI 给出真实反馈。
- 新增文件按职责放入现有目录。只有出现稳定复用需求时才增加 `components/`、`common/` 等新层级。
- 不为小范围修改引入新框架、状态管理库或网络库。
- 中文源文件和 Markdown 使用 UTF-8；PowerShell 输出乱码时使用 `Get-Content -Encoding utf8` 核对，不能仅凭终端显示判断文件已损坏。

## 6. UI、资源与无障碍

- 保持现有 ArkUI/HDS 导航结构：五个底部标签页由 `HdsTabs` 管理，发布按钮位于中间标签。
- 首页顶部采用操作块组件，不设置标题组件，也不设置右上角头像或个人资料区域；新增或调整首页布局时必须保持这一信息架构。
- UI 改动同时检查手机和平板；避免只依赖单一固定屏宽。固定格式控件应使用稳定尺寸或响应式约束，防止内容切换导致布局跳动。
- 保持当前视觉语言：白色主背景、中性色文本、Pinter 红色强调色和克制的圆角。不要无任务依据重做整体主题。
- 优先复用 `entry/src/main/resources/base/media/` 内现有 Lucide 图标与品牌资源，不手绘重复图标。
- 新增图标按钮、无文字控件和关键可点击区域时补充准确的 `accessibilityText`。
- 文本需要设置合理的宽度、最大行数或溢出策略，确保中文和长名称不遮挡相邻内容。
- 需要深浅色差异的颜色应进入资源文件；修改颜色时同步检查 `base` 与 `dark` 资源。
- 面向用户的稳定文案优先放入资源；局部原型文案若继续内联，必须保持 UTF-8 并与真实功能一致。

## 7. 典型变更决策

- 修改灵感字段：先改 `InspirationModels.ets`，再更新内容服务、页面消费点和仓库单元测试。
- 修改本地画板行为：保持已有画板 ID 和存储键含义，除非同时处理兼容迁移。
- 修改收藏/画板行为：沿 `Index.ets -> BoardStorageService -> Preferences` 检查完整读写路径，不在页面另建存储源。
- 修改标签页或详情层：确认 `selectedPin`、`showBoardPicker`、`showNewBoardDialog` 的关闭路径以及遮罩点击行为。
- 增加真实上传、消息、账号或联网能力：先定义服务接口、权限、错误状态、隐私边界和测试，不直接把占位按钮改成虚假成功。
- 修改版本：同步更新 `AppScope/app.json5` 中的 `versionName`、`versionCode`，并确认对外显示位置。
- 修改设备范围或 SDK：同时检查根 `build-profile.json5`、`module.json5` 和所用 API 的兼容性。

## 8. 测试与构建

### 测试位置

- 本地单元测试入口：`entry/src/test/List.test.ets`。
- 业务单元测试：`entry/src/test/InspirationRepository.test.ets`。
- 设备测试入口：`entry/src/ohosTest/ets/test/List.test.ets`。
- 当前模板测试仍较少。修改仓库数据、搜索、画板状态或序列化逻辑时，应补充针对实际行为的测试，不只依赖模板断言。

### 当前可用路径

- 使用 DevEco Studio 的 Build、Run、Test 流程进行编译、安装与测试。
- 本仓库当前没有 `hvigorw`/`hvigorw.bat`，并且 `hvigor`、`ohpm`、`hdc` 不在当前 PowerShell `PATH`。不要在交付记录中声称执行了不存在的命令。
- 若后续恢复项目 wrapper 或明确配置 DevEco Studio 的 CLI 路径，再将经过实际验证的命令补充到本文件。
- 根 `build-profile.json5` 中 `signingConfigs` 当前为空，同时产品引用 `default` 签名配置。编译通过不代表签名、安装或设备运行通过；报告时分开说明源码编译、打包签名、安装启动和设备验证结果。

纯 Markdown 修改通常不需要运行 HAP 构建，但必须验证文档内容、路径和编码。ArkTS、资源或构建配置变化应按风险运行相应单元测试和 HAP 构建。

## 9. 生成文件与修改范围

- 不编辑或提交 `.idea/`、`.hvigor/`、`oh_modules/`、`build/`、`.preview/` 等工具、依赖或构建产物作为业务实现。
- `local.properties` 是 DevEco Studio 自动生成的本机配置，不手工维护，也不写入仓库说明中的绝对 SDK 路径。
- 不顺手重构与任务无关的页面、资源或模板测试。
- 工作区可能已有用户改动；修改前检查目标文件，保留并兼容不属于当前任务的内容。
- 不使用破坏性 Git 命令，不重置或覆盖用户改动。

## 10. 交付要求

完成任务时应说明：

1. 修改了哪些行为或文档，以及对应文件。
2. 实际运行了哪些检查、测试或构建，结果是什么。
3. 哪些验证未执行，以及原因。
4. 如存在签名、设备、环境或工具链阻塞，将其与源码问题分开报告。

交付前至少进行一次范围检查，确保没有把占位能力描述为真实能力，没有改动生成目录，也没有泄露本机路径、凭据或敏感数据。

---

# 跨项目通用协作与验证附录

本附录整理 `zapnote2`、`pinterest` 和 `wordcard` 的共通工作方法。项目专属章节、当前代码和用户最新要求优先级更高；本附录不把某个项目的文件名、能力或临时 workaround 当成其他项目的事实。

## 1. 对话与需求判断

- 先复述用户真正想改变的可观察结果，再判断用户给出的做法是否只是表层方案。目标不清且会改变架构、数据、权限或发布范围时，先说明判断依据和影响面。
- 用户后续缩小范围、指定保留项或要求回滚时，以最后一次明确边界为准。只回滚本次引入的行为，不顺手恢复更早被移除的功能，也不清理无关代码。
- 实现前说明：目标、涉及的页面/协调器/服务、可能受影响的状态或数据、验证路径。小型文案或局部样式改动可简化为一句话。
- 证据分级要明确：源码检查只能证明代码结构，编译只能证明编译面，打包不能证明签名，安装不能证明所有交互，真机和真实远端操作才是设备/网络行为证据。
- 不把演示数据、占位入口、空回调或本地模拟状态描述为真实账号、上传、消息、云同步、备份或服务端能力。
- 发现需求与现有文档、协议或 UI 不一致时，指出偏差和选择：改代码、改文档，或记录暂不支持；不要默默扩大承诺。

## 2. 修改前的快速勘察

1. 阅读当前仓库的 `AGENTS.md`、相关规范和目标文件，确认 SDK、设备范围、入口和测试目录。
2. 运行 `git status --short`，保留用户已有改动；不要用 `reset --hard`、`checkout` 等破坏性命令覆盖工作区。
3. 用 `rg` / `rg --files` 查找模型、页面入口、服务、路由、资源、Preferences key、数据库表和测试引用。删除或重命名功能时，先做全局零匹配检查。
4. 找到真正的状态源和写入边界：页面只协调交互，模型定义契约，Service 负责持久化/网络/系统能力；不要在页面复制第二套存储。
5. 把修改拆成最小闭环：入口、状态、消费方、持久化/副作用、错误状态、文案/帮助、测试。每一步完成后重读精确的修改区。

## 3. 通用 Bug 定位路径

先记录稳定的复现步骤、预期结果、实际结果、设备/系统/数据前置条件，再沿下面的真实链路追踪：

`用户操作 -> UI 回调 -> 父级协调器/路由 -> Service 或系统 API -> 持久化/远端副作用 -> 状态回传 -> 可见 UI`

按症状分类处理：

- **点击无效或状态不变**：检查点击区域、遮罩和 hit-test，再追父级回调、状态重新赋值、props 传递、列表 key，以及 HDS/TabContent 是否缓存旧子树。必要时使用明确的 remount key，不要盲目增加全局存储镜像。
- **设置重启后丢失**：核对默认值、Preferences key、读取时机、保存返回值、异常反馈和重启加载；新增设置还要检查备份/恢复、同步范围、指南和深浅色。
- **数据错、统计错或恢复异常**：先核对模型字段和时间口径，再检查序列化兼容、旧数据迁移、去重、空数据回退、备份 schema 和恢复策略。数据库/文件存在时，不要用“加载为空”触发旧数据回退。
- **网络/同步显示成功但没有结果**：必须检查真实请求方法、URL/远端路径、目录创建、响应码、文件内容和服务端状态。处理 `ArrayBuffer`、BOM、空响应、HTML 错误页、非 JSON 和 schema 不匹配，并保留具体错误原因。
- **异步反馈过早结束**：确认 Promise 是否代表真实完成。例如音频动画应等待播放器完成或超时；上传、导出和导入应在文件/远端副作用完成后再显示成功；所有路径在 `finally` 清理 loading、定时器和临时状态。
- **OCR/文件选择失败**：从 picker URI、文件描述符读取、UTF-8 解码、图像解码到识别/解析阶段逐段记录错误。源码和构建不能替代真机 picker 往返验证。
- **UI/动效异常**：先区分布局、状态传播、HDS 模板化渲染、材质/系统栏、手势和真正帧表现。保留官方 HDS/ArkUI 组件和用户已确认的交互，不用禁用交互来掩盖问题。
- **只在打包末尾失败**：分别查看 `CompileArkTS`、`PackageHap`、`SignHap`、安装和启动阶段。`bundleName`/SigningConfigs、证书、Profile、UDID 等属于环境或发布阻塞，不要归咎于已通过编译的业务改动。

修复原则：先修根因，避免只改 toast/标签；优先小范围、可回滚、符合现有架构的改动；修复后做同一复现路径和相邻回归路径，确认没有重新引入已移除能力。

## 4. 测试分层与最小覆盖

### 静态与源码检查

- 检查新增/删除符号的全局引用、路由注册、资源名大小写、权限、Preferences key、数据库版本和备份字段。
- 检查文案与实际执行路径一致，未接入能力明确写“演示/占位/暂不可用”。
- 中文 Markdown/源码使用 UTF-8；Windows 文档若已有 BOM 约定，检查前三个字节为 `239,187,191`，并用 `Get-Content -Encoding utf8` 复读。
- 检查没有把密码、API Key、令牌、签名秘密写入源码、日志、Preferences、备份、截图或测试夹具。

### 单元测试

优先覆盖不依赖 UI 的纯逻辑和边界：

- 模型默认值、序列化/反序列化、旧格式兼容、重复 ID 和空数据。
- 搜索过滤、排序、分页/limit、日期与统计口径、学习/复习状态转换、批次尾部合并。
- Preferences 默认值、保存失败、重启读取；数据库新装建表、迁移、清空和恢复。
- CSV/JSON 合法输入、空字段、非法字段、重复项、BOM 和错误输入。
- 网络响应转换：字符串/`ArrayBuffer`、BOM、空内容、HTML、非 JSON、错误码和 schema 校验。

测试应在前置清理、后置恢复后运行，避免污染手工验证环境；不要只保留模板断言，应为本次修改补行为断言。

### HAP 构建与包检查

- 优先使用仓库实际存在的 `hvigorw`/DevEco Studio wrapper；命令不存在时记录未执行，不凭空声称通过。
- 读取构建输出中的第一条真实 ArkTS 错误；区分无关 warning、编译、打包和签名错误。
- 记录实际 HAP 路径、生成时间和是否签名。未签名包可证明部分源码/打包结果，但不能证明发布或真机安装。

### 真机/模拟器回归

至少覆盖：首次安装和冷启动、升级安装、清除数据后初始化、核心导航、空/加载/成功/失败/重复点击、键盘和返回、深浅色、系统大字体、窄屏/平板、可访问性标签。

涉及具体能力时增加真实闭环：

- Preferences/数据库：修改 -> 退出或重启 -> 读取仍正确。
- 导入/导出/备份：选择真实文件 -> 读写真实路径 -> 重新导入/恢复并核对数据和资源。
- WebDAV/网络：验证连接、目录创建、上传、下载、远端文件内容和错误提示；使用测试账号，绝不回显凭据。
- OCR/图片/双触/系统分享/AccountKit/防窥/音频：验证权限、系统回调、取消和异常路径；源码或 HAP 通过不能替代设备验证。
- UI/动效：确认用户看到的尺寸、层级、跟手、可打断、状态刷新和遮罩行为，而非只检查属性存在。

## 5. 回归与发布优先级

- **P0，阻塞发布**：无法启动、核心数据丢失/损坏、数据库初始化失败、release 包无法安装、敏感信息泄露。
- **P1，发布前修复**：核心统计或筛选错误、设置不保存、导入崩溃、同步结果错误、深色模式不可读、关键导航失效。
- **P2，按版本修复**：非核心文案、局部间距、弱网提示、特定设备轻微布局偏移。
- **P3，排期处理**：非必要的新能力、缓存和大规模重构。版本冻结后只接受 Bug、性能、兼容性、测试和发布文档改动。

功能删除或回滚后，用原标识、旧入口、恢复逻辑、启动默认值、网络分发和备份字段做零匹配/回归检查；不能只从下拉框或按钮上删除。

## 6. 交付记录模板

最终说明按事实分开写：

1. 改了什么行为/文档，涉及哪些文件。
2. 复现根因和采用的修复路径。
3. 实际运行的源码检查、单元测试、`assembleHap`、签名、安装和真机/远端验证，各自结果。
4. 未执行的验证及原因，尤其是工具链、签名、设备、网络或账号阻塞。
5. 仍存在的风险、占位边界和建议的下一步回归路径。

不要把“代码看起来正确”“编译通过”或“toast 显示成功”写成真实用户行为已验证。
